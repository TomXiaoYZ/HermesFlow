[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "hermes-common"
version = "0.1.0"
description = "Shared utilities for HermesFlow"
dependencies = [
    "pydantic",
    "pydantic-settings",
]
