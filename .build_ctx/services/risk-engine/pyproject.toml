[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "risk-engine"
version = "0.1.0"
description = "HermesFlow Risk Engine"
dependencies = [
    "fastapi",
    "uvicorn",
    "hermes-common",
]
