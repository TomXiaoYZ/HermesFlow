[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "strategy-engine"
version = "0.1.0"
description = "HermesFlow Strategy Engine"
dependencies = [
    "fastapi",
    "uvicorn",
    "hermes-common",
]
