version: '3.8'

services:
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    restart: unless-stopped
    volumes:
      - redis_data:/data

  timescaledb:
    image: timescale/timescaledb:latest-pg15
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_PASSWORD=password
    volumes:
      - hermesflow_postgres-data:/var/lib/postgresql/data
    restart: always

  data-engine:
    build:
      context: .
      dockerfile: services/data-engine/Dockerfile
    ports:
      - "8080:8080"
    environment:
      - RUST_ENV=prod
      - DATA_ENGINE__REDIS__URL=redis://redis:6379
      - DATA_ENGINE__POSTGRES__HOST=timescaledb
      - DATA_ENGINE__POSTGRES__PASSWORD=password
      - DATA_ENGINE__SERVER__HOST=0.0.0.0
      - DATA_ENGINE__BIRDEYE__API_KEY=${DATA_ENGINE__BIRDEYE__API_KEY}
      - DATA_ENGINE__HELIUS__API_KEY=${DATA_ENGINE__HELIUS__API_KEY}
      - SOLANA_RPC_URL=${SOLANA_RPC_URL}
      # Add API keys here or in .env
    depends_on:
      - redis
      - timescaledb
    restart: always

  strategy-engine:
    build:
      context: .
      dockerfile: services/strategy-engine/Dockerfile
    environment:
      - RUST_LOG=info
      - REDIS_URL=redis://redis:6379
    depends_on:
      - redis
      - data-engine
    restart: always

  execution-engine:
    build:
      context: .
      dockerfile: services/execution-engine/Dockerfile
    environment:
      - RUST_LOG=info
      - REDIS_URL=redis://redis:6379
      - HTTPS_PROXY=http://host.docker.internal:7897
      - ALL_PROXY=socks5://host.docker.internal:7897
      - SOLANA_PRIVATE_KEY=${SOLANA_PRIVATE_KEY}
    depends_on:
      - redis
    restart: always

  strategy-generator:
    build:
      context: .
      dockerfile: services/strategy-generator/Dockerfile
    environment:
      - RUST_LOG=info
      - REDIS_URL=redis://redis:6379
      - DATABASE_URL=postgres://postgres:password@timescaledb:5432/hermesflow
    depends_on:
      - redis
      - timescaledb
    restart: always

  web:
    build:
      context: .
      dockerfile: services/web/Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_WS_URL=ws://localhost:8080/ws
      # Note: Browser connects to localhost:8080, not internal docker name
    depends_on:
      - data-engine
    restart: always

volumes:
  redis_data:
  hermesflow_postgres-data:
    external: true
